Title: [comment for #1118] bamCoverage --MNase: four bases for odd fragment lengths comes from true division; one-token fix

<!-- deeptools/deepTools .github/ISSUE_TEMPLATE.md checklist; this text is a comment for the existing open issue #1118, not a new issue -->

- [x] Search whether this issue (or a similar issue) has been solved before: this is #1118 (2022-01-27, still open); the cause and fix below are for that thread.
- [x] deepTools version: 3.5.6 (`master` @ `ea0f68bb`; also reproduced on 3.5.1 and 3.3.1); Python 3.12.3
- [x] Full command producing the issue: `bamCoverage -b mn.bam -o mn.bg -of bedgraph --MNase -bs 1` (script below)
- [x] Output printed: see below

**Cause.** `CenterFragment.get_fragment_from_read` (`deeptools/bamCoverage.py`, lines 405-420):

```python
if read.tlen % 2 == 0:
    fragment_start = read.pos + read.tlen / 2 - 1
    fragment_end = fragment_start + 2
else:
    fragment_start = read.pos + read.tlen / 2 - 1
    fragment_end = fragment_start + 3
```

Under Python 3, `/` is true division, so for an odd template length `fragment_start` is a half-integer (e.g. 172.5 for a 149-bp fragment at 100). `countReadsPerBin.get_coverage_of_region` then floors the start bin and ceils the end bin (lines 697-698), and with `--binSize 1` the window [172.5, 175.5) covers the four bins 173, 174, 175, 176 — the centre base 174 plus one base to its left and two to its right. Under Python 2 the same expression was an integer division and gave the documented three bases (173-175). Even lengths are unaffected, as the issue reports.

**Minimal reproduction** (one proper pair, TLEN 149, fragment 100-248, centre base 174):

```python
import subprocess, pysam

hdr = {"HD": {"VN": "1.0"}, "SQ": [{"SN": "chr1", "LN": 1000}]}
with pysam.AlignmentFile("/tmp/mn.u", "wb", header=hdr) as fh:
    for flag, start, tlen in [(99, 100, 149), (147, 199, -149)]:
        a = pysam.AlignedSegment(); a.query_name = "p"; a.query_sequence = "A" * 50; a.flag = flag
        a.reference_id = 0; a.reference_start = start; a.mapping_quality = 30; a.cigar = ((0, 50),)
        a.query_qualities = pysam.qualitystring_to_array("I" * 50)
        a.next_reference_id = 0; a.next_reference_start = 199 if flag == 99 else 100; a.template_length = tlen
        fh.write(a)
pysam.sort("-o", "/tmp/mn.bam", "/tmp/mn.u"); pysam.index("/tmp/mn.bam")
subprocess.run(["bamCoverage", "-b", "/tmp/mn.bam", "-o", "/tmp/mn.bg", "-of", "bedgraph", "--MNase", "-bs", "1"], check=True, capture_output=True)
covered = [line.split("\t")[1:3] for line in open("/tmp/mn.bg") if not line.rstrip().endswith("\t0")]
print("covered interval(s):", covered)
assert covered == [["173", "176"]], "expected [['173', '176']] (three bases 173, 174, 175)"
```

**Output** (3.5.6):

```
covered interval(s): [['173', '177']]
Traceback (most recent call last):
  File "mcve_dt4_mnase.py", line 18, in <module>
    assert covered == [["173", "176"]], "expected [['173', '176']] (three bases 173, 174, 175)"
AssertionError: expected [['173', '176']] (three bases 173, 174, 175)
```

**Fix.** `read.tlen // 2` in both branches; a patch with a regression test on the shipped `test_paired2.bam` (its 147-bp and 154-bp pairs cover the odd and even cases) is ready and a PR follows.

Found in a source-level correctness audit of research software (methods and harnesses: https://github.com/cindykrafft/research-software-audit/tree/main/audits/deeptools)

---
_Generated by [Claude Code](https://claude.ai/code)_
