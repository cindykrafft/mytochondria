# PR bodies (deeptools/deepTools `.github/PULL_REQUEST_TEMPLATE.md`)

Each patch is one commit on top of `master` @ `ea0f68bb` (the repository's default
branch is `master`; CONTRIBUTING.md asks for feature work on `develop`, which no
longer exists on the remote, so bug-fix branches are cut from `master`). Every patch
carries a regression test that fails on unmodified `master` and a bullet in
`CHANGES.txt` under a new `unreleased` heading (the project has no other changelog
convention; drop or rename the heading as you prefer — the seven bullets conflict
textually with each other in `CHANGES.txt` only). `#NNN` is the issue number once
it exists. The template's four checkboxes are answered in each body.

---

### PR 1 — `fix/bamcompare-skipzerooverzero-coordinates` — "bamCompare: keep bin coordinates after a --skipZeroOverZero gap"

- [ ] Does the PR contain new feature? No
- [x] Does the PR contain bugfix? Yes — fixes #1108 and #1130
- [x] Does the PR contain documentation changes? A `CHANGES.txt` bullet only
- [ ] Does the PR contain changes to the galaxy wrapper? No

`writeBedGraph_worker` derives the coordinates of every written interval from the end of the previous one, so a bin skipped by `--skipZeroOverZero` shifted all following intervals of the chunk one bin to the left, and a skipped bin between two equal values was merged into the run (#1108, #1130). The bigWig-input loop in `writeBedGraph_bam_and_bw.py` already resets on the skip; this makes the BAM loop do the same (flush the current run, restart from the next bin's own coordinate). `writeStart`/`writeEnd` are initialised before the loop.

Test: `test_bam_compare_ZoverZ_interior_bins` runs `--operation first --skipZeroOverZero` on `test_filtering.bam` (a gap between positions 57 and 78, 10-bp bins) and compares with the bamCoverage track of the same file with its zero bins removed; fails on `master` (1 failed), passes with the patch. `test_bamCoverage_and_bamCompare.py` + the multiBigwigSummary and countReadsPerBin modules: 54 passed; full suite 100 passed, 1 pre-existing failure (`test_plotCoverage_default`, also on unmodified `master`); `flake8` with the project's options clean.

Found in Mytochondria, a volunteer project that checks the numerical core of research software and verifies every finding by execution (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/deeptools)

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### PR 2 — `fix/plotpca-log2-rowcenter` — "plotPCA: apply --log2 and --rowCenter to the matrix used for the SVD"

- [ ] Does the PR contain new feature? No
- [x] Does the PR contain bugfix? Yes — fixes #NNN
- [x] Does the PR contain documentation changes? A `CHANGES.txt` bullet only
- [ ] Does the PR contain changes to the galaxy wrapper? No

`plot_pca` applied `--log2` by rebinding `self.matrix` and `--rowCenter` in place on `self.matrix`, while the SVD ran on `m`, the row-selected copy made for `--ntop`. `--log2` therefore never changed the result and `--rowCenter` only did with `--ntop 0`. Both transforms now act on `m` (which also removes the `UFuncOutputCastingError` on integer matrices). Results without those options are unchanged.

Test: new `deeptools/test/test_correlation.py::test_plotPCA_log2_and_rowCenter_change_the_result` compares the `--outFileNameData` loadings with a numpy implementation of the documented pipeline for the default, `--log2`, `--rowCenter` and `--ntop 100 --log2 --rowCenter` (up to sign); fails on `master`, passes with the patch; full suite 100 passed + the pre-existing plotCoverage failure; `flake8` clean.

Found in Mytochondria, a volunteer project that checks the numerical core of research software and verifies every finding by execution (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/deeptools)

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### PR 3 — `fix/plotcorrelation-mad-outliers` — "plotCorrelation: use the median absolute deviation in --removeOutliers"

- [ ] Does the PR contain new feature? No
- [x] Does the PR contain bugfix? Yes — fixes #NNN
- [x] Does the PR contain documentation changes? A `CHANGES.txt` bullet only
- [ ] Does the PR contain changes to the galaxy wrapper? No

`get_outlier_indices` scaled the deviation from the median by `1.4826 * median(|x|)` — the median itself for counts — instead of `median(|x - median|)`, the MAD its docstring and the `--removeOutliers` help name. Bins were only flagged beyond ~300× the median. One line. **Behaviour change to flag in review:** `--removeOutliers` will now actually remove bins that are more than 200 MADs from the median in every sample, so Pearson values change for data with such bins (that is the option's purpose); the default (no option) is unchanged.

Test: new `deeptools/test/test_correlation_outliers.py` (three planted bins at 50× the median among 5,000 Poisson(100) bins are flagged, none on clean data); fails on `master`, passes with the patch; full suite 100 passed + the pre-existing plotCoverage failure; `flake8` clean. (If PR 2 is merged first, this test can move into its `test_correlation.py`.)

Found in Mytochondria, a volunteer project that checks the numerical core of research software and verifies every finding by execution (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/deeptools)

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### PR 4 — `fix/mnase-odd-fragment-centre` — "bamCoverage --MNase: three centre bases for odd fragment lengths"

- [ ] Does the PR contain new feature? No
- [x] Does the PR contain bugfix? Yes — fixes #1118
- [x] Does the PR contain documentation changes? A `CHANGES.txt` bullet only
- [ ] Does the PR contain changes to the galaxy wrapper? No

`read.tlen / 2` is true division under Python 3; for odd template lengths the fragment start became a half-integer and, after the floor/ceil in `get_coverage_of_region`, four bases were counted (one left of the centre, two right). `//` restores the three central bases the help documents (#1118). Even lengths are unchanged.

Test: `test_bam_coverage_MNase_odd_fragment_length` on the shipped `test_paired2.bam` (147-bp pairs → 3 bases, 166/154-bp pairs → 2 bases); fails on `master`, passes with the patch; full suite 100 passed + the pre-existing plotCoverage failure; `flake8` clean.

Found in Mytochondria, a volunteer project that checks the numerical core of research software and verifies every finding by execution (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/deeptools)

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### PR 5 — `fix/ignoreduplicates-scale-factor` — "bamCoverage/bamCompare: count --ignoreDuplicates in the normalisation denominator"

- [ ] Does the PR contain new feature? No
- [x] Does the PR contain bugfix? Yes — fixes #NNN
- [x] Does the PR contain documentation changes? A `CHANGES.txt` bullet only
- [ ] Does the PR contain changes to the galaxy wrapper? No

`fraction_kept` returned 1.0 early unless a MAPQ, SAM-flag or fragment-length filter was set and never looked at `--ignoreDuplicates`, so with `--ignoreDuplicates` alone the per-bin counts were deduplicated but the mapped-read count used by `--normalizeUsing` (CPM/RPKM/RPGC/BPM) and `--scaleFactorsMethod readCount` was not; `--exactScaling` could not override it. One condition added. **Behaviour change to flag in review:** tracks made with `--ignoreDuplicates` and no other filter will scale up by 1/(1 − duplicate fraction).

Test: `test_get_num_kept_reads_ignore_duplicates` on `test_filtering.bam` (position duplicates): the kept-read count with `--ignoreDuplicates` must be below the total and equal the count with an added no-op `--samFlagExclude 4`; fails on `master`, passes with the patch; full suite 100 passed + the pre-existing plotCoverage failure; `flake8` clean.

Found in Mytochondria, a volunteer project that checks the numerical core of research software and verifies every finding by execution (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/deeptools)

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### PR 6 — `fix/multibigwigsummary-exact-stats` — "multiBigwigSummary: compute bin means from the exact bigWig values"

- [ ] Does the PR contain new feature? No
- [x] Does the PR contain bugfix? Yes — fixes #NNN
- [x] Does the PR contain documentation changes? A `CHANGES.txt` bullet only
- [ ] Does the PR contain changes to the galaxy wrapper? No

`countFragmentsInRegions_worker` called pyBigWig's `stats()` with the default `exact=False`, which answers from the zoom level whose span fits the region; on the bigWigs bamCoverage writes, the 10-kb bin values deviated from the true bin means by a median of 3 % and up to several-fold next to peaks (details and the writer-side cause in the issue). `exact=True` reads the intervals themselves, as computeMatrix, bigwigCompare and bigwigAverage already do. **Behaviour change to flag in review:** every multiBigwigSummary value can change; the expected numbers of the existing `test_multiBigwigSummary_gtf` were themselves zoom summaries (27.3125 for the second transcript; the exact mean of its bases is 27.8067) and are updated in this PR. `pyBigWig >= 0.3.2` is needed for the `exact` keyword (the installed 0.3.25 has it; `pyproject.toml` still says `>= 0.2.1`, which you may want to bump). Runtime: the exact path is slower on large bins; if that matters, an `--exact` switch defaulting to on would keep the old speed available.

Test: `test_multiBigwigSummary_bins_exact_mean` writes a 400-kb track with 50-bp steps and a peak next to a bin boundary and compares the 10-kb bin means with numpy; it and the updated `test_multiBigwigSummary_gtf` fail on `master` (2 failed), pass with the patch; full suite 100 passed + the pre-existing plotCoverage failure; `flake8` clean.

Found in Mytochondria, a volunteer project that checks the numerical core of research software and verifies every finding by execution (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/deeptools)

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### PR 7 — `fix/sumcoverage-partial-bins` — "plotFingerprint: credit only the covered bases of a fragment's last bin"

- [ ] Does the PR contain new feature? No
- [x] Does the PR contain bugfix? Yes — fixes #NNN
- [x] Does the PR contain documentation changes? A `CHANGES.txt` bullet only
- [ ] Does the PR contain changes to the galaxy wrapper? No

`SumCoveragePerBin.get_coverage_of_region` added a whole tile to every bin up to the ceiling bin of the fragment end, so a fragment ending inside a bin credited that bin with the full tile and the branch for the partial last bin only ever added `max(0, negative)`. The path is taken when the sampling step equals `--binSize` (one multi-bin region per chunk); the default sampling uses one region per bin and was exact. The three-part bookkeeping is replaced by the per-bin overlap; the doctests in the module still pass.

Test: new `deeptools/test/test_sumCoveragePerBin.py` on `testB.bam` with 40-bp bins (expected sums `[0, 30, 40, 50, 80]`; `master` gives `[0, 30, 60, 60, 80]`); fails on `master`, passes with the patch; full suite 100 passed + the pre-existing plotCoverage failure; `flake8` clean.

Found in Mytochondria, a volunteer project that checks the numerical core of research software and verifies every finding by execution (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/deeptools)

---
_Generated by [Claude Code](https://claude.ai/code)_
