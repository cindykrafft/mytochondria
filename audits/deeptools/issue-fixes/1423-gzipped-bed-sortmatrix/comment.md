The crash comes from the default `--sortRegions keep`: `computeMatrix` re-reads the regions file in `computeMatrixOperations.sortMatrix()` to restore the input order, and while `deeptoolsintervals` opens a gzipped file in binary mode and decodes the first line, `loadBED()` iterates the rest of the handle as `bytes`, so `line.startswith("#")` raises the `TypeError`. `loadGTF()` in the same file already decodes its lines; doing the same in `loadBED()` fixes both `computeMatrix -R regions.bed.gz` and `computeMatrixOperations sort -R regions.bed.gz` (workaround meanwhile: `--sortRegions no`, or gunzip the BED). The fix is the `parse.py#L15` pattern from your comment, which is also what `loadGTF()` already does in this file. PR #NNN attached, with tests on gzipped copies of the existing test BEDs that fail on `master`.

Found while working through open issues in Mytochondria, a volunteer project that verifies fixes for the software behind published results (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/deeptools)

---
_Generated by [Claude Code](https://claude.ai/code)_
