The cause is in `BedFlank::FlankBed()` (`src/flankBed/flankBed.cpp`): with `-s`, a record is flanked only if its column-6 strand is exactly `+` or `-`; any other value (a `.` strand, or a BED3/4/5 file with no strand column) matches neither branch and is silently dropped, which is why `-s` gives no output. The fix treats every non-`-` record as forward, as `slop -s` already does, and adds tests for BED3 and `.`-strand input. PR #NNN attached.

(For the exact line in the report — a 6-column BED with `-` in column 6 — current master flanks it correctly under `-s`, so if it still fails for you, please check that the file is tab-delimited and that the strand is in column 6.)

---
_Generated by [Claude Code](https://claude.ai/code)_
