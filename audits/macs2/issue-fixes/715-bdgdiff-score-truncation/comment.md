The cause is in `TwoConditionScores.mean_from_peakcontent` (`MACS3/Signal/ScoreTrack.py`): after the pure-Python-mode conversion the per-interval log10LR `tmp_v` and its accumulator `sum_v` are declared `cython.long` (they were `float64_t` in 2.2.9.1 and 3.0.0), so every log10 likelihood ratio is cut to its integer part before the length-weighted mean is taken — with `-C 0.2` all values in the common file (|log10LR| ≤ 0.2) become 0 and cond1/cond2 scores can drop below the cutoff, and with the default `-C 3` every score is biased low by up to 1. Declaring both as `cython.double` restores the exact scores in `test/standard_results_bdgdiff` (638 of 638 differ on `main`, 0 with the fix).

PR #NNN attached.

---
_Generated by [Claude Code](https://claude.ai/code)_
