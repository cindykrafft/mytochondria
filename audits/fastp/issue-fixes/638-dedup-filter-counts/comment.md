The discrepancy is in how the filter result is recorded: with `--dedup`, a duplicate is dropped from the output, but its filter category was already recorded as `PASS_FILTER` before the dedup flag is consulted (`seprocessor.cpp` / `peprocessor.cpp`), so "reads passed filter" and `filtering_result.passed_filter_reads` count reads that were never written, while `summary.after_filtering` is computed from the written reads and is right. Reproducible on `master` with 4,000 synthetic reads of which 2,000 are duplicates: `passed_filter_reads` 4,000, `after_filtering.total_reads` 2,000, 2,000 reads in the file; the same code path also means `--merge --dedup` never removes a duplicate.

PR #NNN attached: it records such reads under a new `FAIL_DUPLICATE` category (`reads failed due to duplication` / `"duplicated_reads"` / `reads duplicated`, only when `--dedup` is on), so `passed_filter_reads` equals what is written, with a regression script in `scripts/`.

Found while working through open issues in Mytochondria, a volunteer project that verifies fixes for the software behind published results (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/fastp)

---
_Generated by [Claude Code](https://claude.ai/code)_
