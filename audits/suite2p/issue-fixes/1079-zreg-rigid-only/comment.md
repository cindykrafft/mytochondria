Part 1 of this still reproduces on `main` (90be895, v1.1.0), with a different error than in v0.14.2: `register.registration_wrapper(..., refImg=[img0, img1, ...])` with `nonrigid=False` fails in `shift_frames` with `IndexError: list index out of range` (`register.py:459`). The `nZ > 1` branch of `compute_shifts` always returns six offset tensors, so the nonrigid entries are zero-width `(n_frames, 0)` tensors instead of `None`, and `shift_frames` then calls `nonrigid.transform_data` with the empty block list that `register_frames` builds when `block_size=None`; the single-reference branch returns `None` for those entries and takes the rigid-only path. Part 2 (two channels, `nonrigid=True`) runs on `main`.

A branch with a six-line fix in `compute_shifts` (return `None` nonrigid offsets when the reference has no nonrigid masks, as the `nZ == 1` path does) and a unit test that registers a synthetic movie against two reference images with `nonrigid` on and off is at https://github.com/cindykrafft/suite2p/tree/fix/issue-1079-zreg-rigid-only. Happy to open a PR if that is useful, or leave it with you since the issue is assigned.

Found while working through open issues in a source-level correctness audit of research software (methods and harnesses: https://github.com/cindykrafft/research-software-audit/tree/main/audits/suite2p)

---
_Generated by [Claude Code](https://claude.ai/code)_
