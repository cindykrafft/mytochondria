The cliff is the small-data threshold: for `metric="precomputed"`, `nearest_neighbors()` returns no search index, and once the training set has 4096 or more rows (the non-"small" path) `fit()` stores `_knn_search_index = None`, which trips the #848 "No search index available" guard at the top of `transform()` before the precomputed branch, which needs no index, is reached. Below 4096 rows that attribute is never set, so the same call works. PR #NNN attached: it skips the guard for the precomputed metric and adds a regression test.

---
_Generated by [Claude Code](https://claude.ai/code)_
