Confirmed on current master (9c4a3af): `ft_freqanalysis.m` uses the percentage in `cfg.toi` directly as the step between time points, so `'10%'` gives 90 % overlap and `'90%'` gives none, and only `'50%'` matches the help text. The fix is the one you proposed, step = `(1-overlap)*min(cfg.t_ftimwin)`, plus an error for percentages of 100 or more (which would otherwise request an infinite time axis) and a `test_issue2345.m` that fails on master and passes with the change. PR #NNN attached.

---
_Generated by [Claude Code](https://claude.ai/code)_
