The error comes from `sanity_test_report_unknown_proteins` in `db_utils.py`, not from your files: when no complex in `complex_input.csv` uses one of the `uniprot_N` columns in the header (e.g. `uniprot_3`/`uniprot_4`/`uniprot_5` in a file where every complex is a dimer), pandas reads that column as all-NaN `float64`, and the sanity test merges it against the string `uniprot` column of `protein_input.csv`, which pandas refuses. Converting the columns to strings before saving does not help because `create_db` re-reads the CSVs; the workaround until the fix lands is to delete the unused `uniprot_N` columns from the header of `complex_input.csv`.

This is the same traceback as #137 (2023). PR #NNN attached: it makes the check dtype-independent (same warnings, same output) and adds a test that builds a database from files with empty subunit columns.

Found while working through open issues in Mytochondria, a volunteer project that verifies fixes for the software behind published results (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/cellphonedb)

---
_Generated by [Claude Code](https://claude.ai/code)_
