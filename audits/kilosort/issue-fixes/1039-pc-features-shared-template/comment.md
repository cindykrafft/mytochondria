Confirmed on `main` (17743f2) with your example: `make_pc_features` selects spikes by detection template from the full `spike_templates`/`tF`, so processing cluster 0 also re-orders (in place) the `tF` rows of cluster 1's spikes, which are then re-ordered a second time, from the wrong starting order, when cluster 1 is processed. Restricting the selection, the channel ranking and the in-place write to the current cluster's spikes fixes it and leaves clusters that do not share a template byte-identical; PR #NNN attached, with your example as a regression test.

---
_Generated by [Claude Code](https://claude.ai/code)_
