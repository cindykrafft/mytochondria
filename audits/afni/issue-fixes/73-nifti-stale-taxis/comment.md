This reproduces on current master with a synthetic file, no MRIQC needed. The "dimensions altered since AFNI extension was added" warning is the whole story: the input had been sliced by nibabel, which keeps the AFNI header extension, so the extension's `TAXIS_NUMS` still says 204 volumes while the NIfTI header says 203. AFNI warns but applies the stale extension anyway, leaving `DSET_NUM_TIMES` = 204 next to `DSET_NVALS` = 203 (your `3dinfo` shows exactly that pair), and the NIfTI writer refuses the mismatch with "NBL does not match nim"; `3dvolreg` never checked the write status, hence exit 0 and the 0-byte file.

PR #NNN attached: the time axis is limited to the sub-bricks actually present when the extension disagrees with the header, and `3dvolreg` now exits non-zero if a write fails.

---
_Generated by [Claude Code](https://claude.ai/code)_
