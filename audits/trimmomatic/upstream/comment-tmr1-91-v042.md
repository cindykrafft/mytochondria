Title: (comment on #91) retargeted to V0.42, POM change dropped

<!-- Reply to sebeier's comment of 2026-09-25 06:52 UTC on PR #91 ("Could you retarget this to the V0.42 branch so it makes it into the next release? And please drop the change to dependency-reduced-pom.xml, it seems to only change line endings from a local build."); the same morning sebeier confirmed on #90 that palindrome mode should charge Q/10 and that the fix is planned for 0.42. Post only after (1) the PR base is changed to V0.42 in the GitHub UI ("Edit" next to the title) and (2) the prepared branch is force-pushed: fix/palindrome-mismatch-penalty = 54af058, one commit on origin/V0.42 @ 6bd48b5 touching IlluminaClippingTrimmer.java and the new test only. Facts checked 2026-09-25: on V0.42 `mvn -B clean verify` (JDK 25.0.4) passes with 517 tests before and 519 with the change, and the new test fails without it (testTwoMismatchesAtQ19StayBelowThreshold); IlluminaClippingTrimmer.java is identical on main and V0.42 apart from the two changed lines; the shade plugin rewrites dependency-reduced-pom.xml on every build, which is how it got into the first commit; build-and-test.yml triggers on push/pull_request to main only. -->

Thanks! Done: the branch is now a single commit on top of `V0.42` with just the fix and the test, and the `dependency-reduced-pom.xml` change is gone (the shade plugin had rewritten it during my local build).

On `V0.42`, `mvn -B clean verify` passes with 519 tests (517 before), and the new test fails without the fix. Since the Build and Test workflow only runs for pull requests against `main`, there won't be a CI run on this PR now.

Happy to add a line to the 0.42 block of `versionHistory.txt` if you'd like one.

---
_Generated by [Claude Code](https://claude.ai/code)_
