Confirmed on current `main` (`4e20e07`) with the real code under Octave: the `0/0` is in the mass-preserving rescale `rA = times(rA,sum(pA)./sum(rA))`, which is reached whenever a column of the reduced prior has no mass left — either because pruning removed every entry that carried prior mass, or because the prior column is all zeros (the `MI` branch has the same problem for that case). The `NaN` column is then written back into the returned priors, so it corrupts the next epoch rather than only the current call. The suggested guard is right, but rescaling the retained prior by `sum(pA)./sum(rA)` is still `0/0` for an all-zero prior column, so the fix also sets the rescale factor to one for those columns and covers both modes. PR #NNN attached, with a unit test that fails 11/23 assertions before and 0/23 after.

---
_Generated by [Claude Code](https://claude.ai/code)_
