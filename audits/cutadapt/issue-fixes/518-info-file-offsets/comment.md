The cause is that `InfoFileWriter` slices `info.original_read` at the match coordinates, while the coordinates refer to the read after `--cut`/`--quality-cutoff`/`--nextseq-trim` have removed bases from its ends, so everything is shifted by the number of 5'-removed bases (and with `--revcomp`, by the number of 3'-removed bases). The fix records those counts in `ModificationInfo` and lets the writer add the removed bases back to the first record, so offsets and columns 5–7 refer to the original read as documented. PR #NNN attached. It takes the minimal route rather than the `Match`-object idea you floated: `info.matches` is also what `DiscardUntrimmed`/`IsTrimmed` (`predicates.py:153`) and the `{adapter_name}`/`{match_sequence}` renamer placeholders (`modifiers.py:584`, `:775-777`) read, so turning cutters into matches changes those too and settles the info-file row format along the way. The PR says so and I am happy to rework it that way if you prefer.

Found while working through open issues in Mytochondria, a volunteer project that verifies fixes for the software behind published results (methods and harnesses: https://github.com/cindykrafft/mytochondria/tree/main/audits/cutadapt)

---
_Generated by [Claude Code](https://claude.ai/code)_
